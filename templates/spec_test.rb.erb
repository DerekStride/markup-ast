################################################################################
# This file is generated by the specs:generate rake task and
# should not be modified manually.
################################################################################

require "test_helper"

class GeneratedSpecTest < Minitest::Test
  include MarkdownHelper
  include SpecHelper
  <% specs.each do |spec| %>
    def test_<%= spec[:name] %>
      markdown = <<-MARKDOWN
<%= spec[:markdown].lines.join("\n") %>
      MARKDOWN

      html = <<-HTML
<%= spec[:html] %>
      HTML

      assert_equal(html, render(markdown))
    end
  <% end %>
end
